---
import { actions } from "astro:actions"
import Layout from "../layouts/Layout.astro"

const suggestions = await Astro.callAction(actions.e621.getSuggestions, "wulfre")
---

<Layout>
    <pre>{JSON.stringify(suggestions.data, undefined, 4)}</pre>
</Layout>
